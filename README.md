## Usage:
* npm install
* ```
  git checkout master
  ```
* ```
  git pull origin master
  ```
* ```
  npm install
  ```
* ```
  git checkout -b <yourName/fileName>
  ```
* Resolve task
* ```
  git push origin <yourName/fileName>
  ```

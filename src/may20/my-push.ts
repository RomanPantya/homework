/**
 * Create your own implementation of Array.prototype.push()
 * Name your function <myPush>
 * It should work exactly like original method, except:
 *      1. Don't use original method inside
 *      2. It should has required parameter on first position to store client's array
 * 
 * 
 * Example of usage:
 * 
 *      const arr = [1, 3, 4];
 *      
 *      myPush(arr, 'hello world');
 *      console.log(arr); // [1, 2, 4, 'hello world']
 */
 